# 更新日志

本文件记录面向用户的重要变化。尚未发布的内容先写在 **Unreleased**，确定版本号并完成发布后再标记为正式版本。

## Unreleased（相对 v0.1.0）

### 新增与改进

- 重做原有 Home、Diary、Chat、Calendar 页面布局，新增独立 Task 页视觉预览，并将 Web 的 JavaScript 和 CSS 整理为按页面划分的目录。
- 改进 Home 原有的心情、迷你日历和任务清单界面；任务菜单与完成进度展示更清晰，任务仍保存在本机 WebEngine 的本地存储中。
- 更新 Diary 编辑界面与窗口尺寸适配，保留按日期保存、文本快捷标记及未保存内容确认流程。
- 更新 Calendar 的月历布局，继续展示日记与心情标记，并支持点击日期打开日记。
- 桌宠新增系统托盘入口、生病与治疗流程、对应的提示和动画；改进抚摸后恢复自然动作的状态切换。
- 整理桌宠动画、状态面板和 Web UI 的资源路径，并加入新的页面截图与状态演示 GIF。
- 调整 PyInstaller 打包配置，排除不使用的部分 Qt 模块及构建环境混入的 DLL；生成单文件 Windows EXE。

### 行为变化

- 饱食度、心情值和精力值只在程序运行期间自然下降；关闭程序期间不再结算离线下降。
- 饱食度或心情值过低时可进入生病状态，普通互动暂时受限；治疗完成后恢复状态。

### 当前限制

- Chat 仍是固定回复的界面演示，尚未接入 AI API，也不保存聊天记录。
- 独立 Task 页是视觉预览，尚未与 Home 任务清单联动；Focus 与右侧部分统计仍是展示内容。
- Diary 输入时不会自动保存；界面里的 “Auto saved” 文案不能理解为实时自动保存。
- 公开仓库不包含完整原创动画和 Web UI 视觉素材；完整运行版本需通过 Release 下载。

## v0.1.0

- 初次提供 Windows 单文件下载版本。
